meta {
  name: delete_card_again
  type: http
  seq: 3
}

delete {
  url: {{cards_url}}{{created_card}}
  body: none
  auth: inherit
}

assert {
  res.status: eq 404
  res.body: isJson
}

settings {
  encodeUrl: true
  timeout: 0
}
