meta {
  name: get_learning_sets
  type: http
  seq: 3
}

get {
  url: {{learning_set_url}}
  body: none
  auth: inherit
}

assert {
  res.status: eq 200
  res.body: isDefined
  res.body: isNotEmpty
  res.body: isArray
}

settings {
  encodeUrl: true
  timeout: 0
}
