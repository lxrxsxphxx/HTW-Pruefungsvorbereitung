meta {
  name: get_learning_sets_by_name
  type: http
  seq: 1
}

get {
  url: {{learning_set_url}}?modul=test_run
  body: none
  auth: inherit
}

params:query {
  modul: test_run
}

assert {
  res.status: eq 200
  res.body: isDefined
  res.body: isNotEmpty
}

settings {
  encodeUrl: true
  timeout: 0
}
