meta {
  name: update_card
  type: http
  seq: 4
}

put {
  url: {{cards_url}}/api/cards/:id
  body: json
  auth: none
}

params:path {
  id: 13
}

body:json {
  {
    "front": "new",
    "back": "new"
  }
}
