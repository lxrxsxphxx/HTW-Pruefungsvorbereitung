meta {
  name: filter_by_name
  type: http
  seq: 3
}

get {
  url: {{learning_set_url}}?modul=test_run
  body: none
  auth: inherit
}

params:query {
  modul: test_run
}

assert {
  res.status: eq 200
  res.body: isJson
  res.body: isDefined
}

settings {
  encodeUrl: true
  timeout: 0
}
