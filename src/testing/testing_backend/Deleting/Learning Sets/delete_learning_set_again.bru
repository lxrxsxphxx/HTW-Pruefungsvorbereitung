meta {
  name: delete_learning_set_again
  type: http
  seq: 3
}

delete {
  url: {{learning_set_url}}{{created_learning_set}}
  body: none
  auth: inherit
}

assert {
  res.status: eq 404
  res.body: isJson
  res.body.detail: isString
}

settings {
  encodeUrl: true
  timeout: 0
}
