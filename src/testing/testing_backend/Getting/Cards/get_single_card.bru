meta {
  name: get_single_card
  type: http
  seq: 1
}

get {
  url: {{cards_url}}{{created_card}}
  body: json
  auth: none
}

assert {
  res.status: eq 200
  res.body: isJson
  res.body.front: eq {{posted_front}}
  res.body.back: eq {{posted_back}}
  res.body.id: eq {{created_card}}
}
