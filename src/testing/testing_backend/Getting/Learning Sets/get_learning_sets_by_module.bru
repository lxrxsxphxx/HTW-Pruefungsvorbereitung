meta {
  name: get_learning_sets_by_module
  type: http
  seq: 1
}

get {
  url: {{learning_set_url}}?modul={{module}}
  body: none
  auth: inherit
}

params:query {
  modul: {{module}}
}

assert {
  res.status: eq 200
  res.body: isDefined
  res.body: isArray
  res.body: isNotEmpty
}

settings {
  encodeUrl: true
  timeout: 0
}
