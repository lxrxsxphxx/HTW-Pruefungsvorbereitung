meta {
  name: initial_login
  type: http
  seq: 2
}

post {
  url: http://127.0.0.1:8000/api/users/login
  body: json
  auth: inherit
}

body:json {
  {
    "username": "test@htw.de",
    "passwd": "1234"
  }
}

assert {
  res.status: eq 200
  res.body: isJson
  res.body.message: eq "successfully logged in!"
}

settings {
  encodeUrl: true
  timeout: 0
}
